<body>
    <div>
        <h1>Listado de Productos</h1>
        {{#each products }}
        <ul>
            <li>{{this.title}}</li>
            <li>{{this.description}}</li>
            <li>{{this.price}}</li>
            <li>{{this.stock}}</li>
        </ul>
        {{/each}}
    </div>
</body>